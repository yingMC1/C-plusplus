#include <bits/stdc++.h>

#define lowbit(x) x & (-x)
#define endl '\n'
using namespace std;
using i64 = long long;
const int N = 1e5 + 5;

int n, l, r, a[N];

int main() {
    cin >> n >> l >> r;
    for (int i = 1; i <= n; i++) {
    }
    return 0;
}