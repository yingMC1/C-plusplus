#include <bits/stdc++.h>

using namespace std;
using i64 = long long;
const int N = 50005;

int n, a[N], R[N], l[N], s[N], tag[N], bl[N];

int build() {
}

int main() {
    ios::sync_with_stdio(0), cout.tie(0), cin.tie(0);
    return 0;
}